Set up Claude Code configuration for the current project:

1. Create .claude/ directory if it doesn't exist
2. Set up settings.json with appropriate permissions for the project type
3. Create CLAUDE.md with project context and guidelines
4. Configure any project-specific MCP servers
5. Add common slash commands for the project

Guide the user through Claude Code project setup.
