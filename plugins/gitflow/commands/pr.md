Create a pull request for the current branch:

1. Ensure all changes are committed
2. Push the branch to origin if not already pushed
3. Generate a PR description based on commits
4. Create the PR using the GitHub CLI
5. Provide the PR URL to the user

Use conventional commit messages to generate a meaningful PR description.
