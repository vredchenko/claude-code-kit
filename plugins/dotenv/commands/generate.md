Generate environment variable documentation:

1. Scan the codebase for environment variable usage
2. Identify all referenced environment variables
3. Create or update .env.example with all variables
4. Add comments describing each variable's purpose
5. Group variables by feature or service

Document all environment variables used in the project.
